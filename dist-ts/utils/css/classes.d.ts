/**
 * Add class names to an element.
 *
 * @param {Element} element    The element to update.
 * @param {String}      classNames A string of class names.
 * @return {void}
 */
export declare function add(element: Element, classNames: string): void;
/**
 * Remove class names from an element.
 *
 * @param  {Element} element    The element to update.
 * @param  {String}      classNames A string of class names.
 * @return {void}
 */
export declare function remove(element: Element, classNames: string): void;
/**
 * Toggle class names from an element.
 *
 * @param  {Element} element    The element to update.
 * @param  {String}      classNames A string of class names.
 * @return {void}
 */
export declare function toggle(element: Element, classNames: string): void;
